name: Build IP lists

on:
  push:
    branches:
      - main
    paths-ignore:
      - ".gitignore"
      - "LICENSE*"
      - "*.md"
  workflow_dispatch:
  schedule:
    - cron: '0 3 * * *'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Checkout repo
      uses: actions/checkout@v3
      with:
        persist-credentials: false
        
    - name: Setup Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.24'
        
    - name: Tidy go modules
      run: go mod tidy

    - name: Download lst
      run: |
        mkdir -p geo
        rm -rf geo/*
        curl -L "https://raw.githubusercontent.com/Davoyan/ipinfo/main/geo/geoip/ru.lst" -o geo/ru.lst
        curl -L "https://raw.githubusercontent.com/Davoyan/ipinfo/main/geo/geoip/by.lst" -o geo/by.lst
        
    - name: Install geoip tool
      run: |
        go install github.com/Loyalsoldier/geoip@latest
        
    - name: Build dat
      run: |
        geoip convert
        
    - name: Rename
      run: |
        mkdir -p release
        mv release/geoip.dat release/geoipinfo.dat
        sha256sum release/geoipinfo.dat > release/geoipinfo.dat.sha256sum
       
    - name: Generate hash
      run: |
        sha256sum release/geoipinfo.dat > release/geoipinfo.dat.sha256sum
          
    - name: Commit and push generated files
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        git config user.name "IP geoipinfo.dat Generator"
        git config user.email "noreply@myrepo.com"
        git add geo/*
        git add release/*
        git commit -m "Update country IP lists" || echo "No changes"
        git push https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git
